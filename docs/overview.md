# AIDD Plugin - Multi-IDE AI-Driven Development

> **版本**: 0.1.0
> **支持 IDE**: Kimi Code, Cursor, Codex CLI
> **语言**: Python 3.13+
> **许可证**: MIT

---

## 目录

1. [项目概述](#项目概述)
2. [核心特性](#核心特性)
3. [架构设计](#架构设计)
4. [工作流程](#工作流程)
5. [快速开始](#快速开始)
6. [开发状态](#开发状态)
7. [贡献指南](#贡献指南)

---

## 项目概述

AIDD Plugin 是一个**跨平台、多 IDE 兼容**的 AI 驱动开发（AI-Driven Development）框架。它将大语言模型（LLM）从一个"超级大脑"转变为一个遵循传统软件开发生命周期（SDLC）的多角色团队。

### 设计目标

- **IDE 无关**: 同一套工作流，支持 Kimi Code、Cursor、Codex CLI 等多种 AI IDE
- **结构化开发**: Idea → Research → Plan → Implement → Review → QA 的完整流程
- **知识沉淀**: 通过 RLM (Repository Link Model) 建立可复用的代码知识图谱
- **质量保障**: 多阶段质量门禁，确保交付质量

### 与原始 AIDD 的关系

本项目基于 [AIDD for Claude Code](https://github.com/GrinRus/ai_driven_dev) 移植，但做了以下改进：

1. **IDE 抽象层**: 提取 IDE 适配接口，支持多种 AI IDE
2. **简化部署**: 纯 Python 实现，无需复杂的 Claude Plugin 系统
3. **模块化设计**: 每个 Skill 和 Agent 都可以独立使用

---

## 核心特性

### 1. 完整的 SDLC 工作流

```
┌─────────┐    ┌──────────┐    ┌───────┐    ┌───────────┐    ┌──────────┐    ┌──────┐
│  Idea   │ →  │ Research │ →  │ Plan  │ →  │ Implement │ →  │  Review  │ →  │  QA  │
└─────────┘    └──────────┘    └───────┘    └───────────┘    └──────────┘    └──────┘
    │               │              │              │               │             │
  Analyst       Researcher      Planner      Implementer      Reviewer      QA Agent
```

### 2. RLM (Repository Link Model)

代码库知识图谱系统：

- **Nodes**: 文件/目录的语义摘要（功能、公共符号、风险点）
- **Links**: 代码间的关系（调用、引用、继承）
- **Slice**: 按需查询，快速定位相关代码

```bash
# 示例：查询所有与 "payment" 相关的代码
python3 $AIDD_ROOT/skills/aidd-rlm/runtime/rlm_slice.py \
  --ticket FUNC-123 \
  --query "payment|charge|billing" \
  --paths src/services \
  --lang py
```

### 3. 质量门禁系统

每个阶段都有自动化的质量检查：

| 阶段 | 门禁 | 说明 |
|------|------|------|
| Idea | `analyst_check` | PRD 完整性检查 |
| Research | `research_check` | RLM 就绪检查 |
| Plan | `plan_review_gate` | 计划可执行性验证 |
| Implement | `format-and-test` | 代码格式和测试 |
| Review | `diff_boundary_check` | 变更范围检查 |
| QA | `qa_gate` | 最终质量检查 |

### 4. Loop 模式

Implement ↔ Review 的迭代循环：

```
Implement → Review → (REVISE → Implement → Review)* → SHIP
```

自动迭代直到代码质量达标。

---

## 架构设计

### 整体架构

```
┌─────────────────────────────────────────────────────────────────────┐
│                         AI IDE (Kimi/Cursor/Codex)                  │
│                    ┌─────────────────────────────────┐              │
│                    │      /skill:aidd-core           │              │
│                    │      /flow:aidd-idea-flow       │              │
│                    └─────────────────────────────────┘              │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│                         AIDD Plugin Core                            │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐│
│  │   Skills    │  │   Agents    │  │   Runtime   │  │    Hooks    ││
│  │  (14 个)    │◄─┤  (11 个)    │◄─┤  (Python)   │◄─┤  (Shell)    ││
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘│
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│                         Workspace (./aidd/)                         │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐   │
│  │ docs/   │  │reports/ │  │ config/ │  │ .cache/ │  │templates│   │
│  │ prd/    │  │ research│  │ gates   │  │ tests   │  │         │   │
│  │ plan/   │  │ loops/  │  │ conv    │  │         │  │         │   │
│  │ tasklist│  │ qa/     │  │         │  │         │  │         │   │
│  └─────────┘  └─────────┘  └─────────┘  └─────────┘  └─────────┘   │
└─────────────────────────────────────────────────────────────────────┘
```

### 目录结构

```
aidd-plugin/
├── agents/                      # AI Agent 定义
│   ├── analyst.md              # PRD 分析 Agent
│   ├── researcher.md           # 代码研究 Agent
│   ├── planner.md              # 计划制定 Agent
│   ├── implementer.md          # 代码实现 Agent
│   ├── reviewer.md             # 代码审查 Agent
│   └── qa.md                   # QA 检查 Agent
├── skills/                      # Skill 定义
│   ├── aidd-core/              # 核心 Skill
│   ├── aidd-init-flow/         # 初始化 Flow
│   ├── aidd-idea-flow/         # Idea 阶段 Flow
│   ├── aidd-research-flow/     # Research 阶段 Flow
│   ├── aidd-plan-flow/         # Plan 阶段 Flow
│   ├── aidd-implement-flow/    # Implement 阶段 Flow
│   ├── aidd-review-flow/       # Review 阶段 Flow
│   └── aidd-qa-flow/           # QA 阶段 Flow
├── aidd_runtime/               # 核心运行时包
│   ├── runtime.py              # 路径解析、状态管理
│   ├── gates.py                # 质量门禁
│   └── resources.py            # 资源管理
├── skills/                     # Skill 定义 + 各阶段 runtime
│   ├── aidd-core/runtime/      # 核心运行时
│   ├── aidd-rlm/runtime/       # RLM 运行时
│   ├── aidd-loop/runtime/      # Loop 模式运行时
│   ├── aidd-flow-state/runtime/ # 流程状态管理
│   ├── aidd-docio/runtime/     # 文档 IO
│   ├── aidd-observability/runtime/
│   ├── researcher/runtime/     # Research 阶段
│   ├── implement/runtime/      # Implement 阶段
│   ├── review/runtime/         # Review 阶段
│   └── qa/runtime/             # QA 阶段
├── hooks/                       # 自动化钩子（已迁移，编排待收敛）
├── templates/                   # 工作区模板
├── tests/                       # 测试
└── scripts/                     # 开发脚本
    ├── activate.sh             # 环境激活
    ├── install.sh              # 安装脚本
    └── test.sh                 # 测试脚本
```

### Skill 类型

| 类型 | 说明 | 示例 |
|------|------|------|
| **Standard Skill** | 查看帮助文档 | `/skill:aidd-core` |
| **Flow Skill** | 执行工作流 | `/flow:aidd-idea-flow` |
| **Agent Skill** | 执行特定任务 | `/skill:aidd-implementer` |

---

## 工作流程

### 完整工作流示例

```bash
# 1. 初始化 AIDD 工作区（首次使用）
/flow:aidd-init-flow

# 2. 创建新功能（Idea 阶段）
/flow:aidd-idea-flow FUNC-001 "实现用户登录功能"
# → 生成 PRD 文档
# → 提出澄清问题
# → 等待用户回答

# 3. 代码研究（Research 阶段）
/flow:aidd-research-flow FUNC-001
# → 扫描代码库
# → 生成 RLM Pack
# → 输出研究报告

# 4. 制定计划（Plan 阶段）
/flow:aidd-plan-flow FUNC-001
# → 基于 PRD 和 Research 制定实施计划
# → 生成 Tasklist

# 5. 迭代实现（Implement 阶段）
/flow:aidd-implement-flow FUNC-001
# → Loop 模式迭代实现
# → 每个工作项完成后自动提交

# 6. 代码审查（Review 阶段）
/flow:aidd-review-flow FUNC-001
# → 自动代码审查
# → 生成 Review Report
# → REVISE 则返回 Implement，SHIP 则进入 QA

# 7. 质量检查（QA 阶段）
/flow:aidd-qa-flow FUNC-001
# → 运行完整测试
# → 生成 QA 报告
# → 通过则标记为 READY
```

### Loop 模式详解

```
┌─────────────────────────────────────────────────────────────────┐
│                    Loop Iteration                               │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ IMPLEMENT                                               │   │
│  │  1. 读取 Loop Pack (Goal/DoD/Boundaries)               │   │
│  │  2. 读取 Review Pack (如果有反馈)                       │   │
│  │  3. 执行代码变更                                        │   │
│  │  4. 更新进度                                            │   │
│  │  5. 输出 Actions                                        │   │
│  └────────────────┬────────────────────────────────────────┘   │
│                   │                                            │
│                   ▼                                            │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ REVIEW                                                 │   │
│  │  1. 审查代码变更                                        │   │
│  │  2. diff_boundary_check (验证边界)                     │   │
│  │  3. 生成 Review Pack                                    │   │
│  │  4. verdict: REVISE | SHIP | BLOCKED                    │   │
│  └────────────────┬────────────────────────────────────────┘   │
│                   │                                            │
│         ┌─────────┴─────────┐                                 │
│         ▼                   ▼                                 │
│  verdict=REVISE      verdict=SHIP                             │
│         │                   │                                 │
│         └─────────┬─────────┘                                 │
│                   │                                            │
│         继续 Loop           进入 QA                           │
└─────────────────────────────────────────────────────────────────┘
```

---

## 快速开始

### 环境准备

```bash
# 1. 克隆仓库
git clone <repository-url>
cd aidd-plugin

# 2. 激活开发环境
source scripts/activate.sh

# 3. 安装依赖
./scripts/install.sh

# 4. 设置环境变量（添加到 ~/.bashrc 或 ~/.zshrc）
export AIDD_ROOT=/path/to/aidd-plugin
```

### 验证安装

```bash
# 诊断环境
python3 $AIDD_ROOT/skills/aidd-observability/runtime/doctor.py
```

### 在 Kimi Code 中使用

```bash
# 进入项目目录
cd my-project
kimi

# 初始化 AIDD 工作区
> /flow:aidd-init-flow

# 创建新功能
> /flow:aidd-idea-flow FUNC-001 "实现购物车功能"
```

### 在 Cursor 中使用

```bash
# Cursor 中打开项目，在 Chat 中输入：
/skill:aidd-core

# 然后执行 Flow：
/flow:aidd-init-flow
```

---

## 开发状态

### 已完成 ✅

- [x] 项目目录结构
- [x] Python 3.13 虚拟环境 (UV)
- [x] 核心运行时迁移（从 AIDD）
- [x] 基础 Skill 框架
- [x] RLM 运行时
- [x] 流程状态管理
- [x] Schema 定义

### 进行中 🔄

- [x] **Agent 系统** - 11 个标准 Agent 已补齐并加上结构校验测试
- [ ] **Flow Runtime** - 让 Flow 可以真正执行
- [ ] **初始化系统** - 完善 init.py 和模板

### 待实现 ⏳

- [ ] Hooks 系统（自动化门禁）
- [ ] 完整的 Stage Skills
- [ ] Cursor/Codex 适配器
- [ ] 端到端测试
- [ ] 完整文档

---

## 贡献指南

### 开发流程

1. **Fork 仓库** 并创建功能分支
2. **遵循代码风格** - 使用 Black 格式化
3. **添加测试** - 新功能必须有测试覆盖
4. **更新文档** - 修改 AGENTS.md 和相关 SKILL.md
5. **提交 PR** - 描述清楚变更内容

### 添加新 Agent

Agent 文件位于 `agents/{name}.md`，结构如下：

```markdown
---
name: {agent-name}
description: {description}
tools: [Read, Write, Bash, ...]
---

<role>
Agent 职责和核心能力
</role>

<process>
## 1. 步骤一
...
## 2. 步骤二
...
</process>

<output>
输出格式规范
</output>
```

### 添加新 Skill

Skill 位于 `skills/{skill-name}/`，结构如下：

```
skills/{skill-name}/
├── SKILL.md              # Skill 定义
├── runtime/              # 运行时（可选）
│   └── {command}.py
└── templates/            # 模板（可选）
    └── {template}.md
```

### 代码规范

- **Python**: Black 格式化，类型注解
- **文档**: Markdown，中英文之间加空格
- **提交**: 遵循 Conventional Commits

---

## 相关资源

- **原始 AIDD**: https://github.com/GrinRus/ai_driven_dev
- **Kimi Code**: https://github.com/moonshot-ai/kimi-cli
- **Cursor**: https://cursor.sh
- **Codex CLI**: https://github.com/openai/codex

---

## 许可证

MIT License - 详见 [LICENSE](LICENSE)

---

*本文档由 AI 辅助生成，最后更新: 2026-02-16*
